{% extends "base.html.twig" %}
{% block body %}
{% if error %}
    <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
{% endif %}
    <div class="container">
            <form action="{{ path('login') }}" class="form-signin"method="post">
                <h2 class="form-signin-heading text-center">Por favor inicie sesión</h2>
                <label class="sr-only" for="username">Username:</label>
                <input class="form-control" type="text" id="username" name="_username" value="{{ last_username }}" required/>
                <label class="sr-only" for="password">Password:</label>
                <input class="form-control" ty  pe="password" id="password" name="_password" required/>
                <button class="btn btn-block btn-primary" type="submit">login</button>
            </form>
    </div>
{% endblock %}